name: "baseline_evaluation"
description: " v0.1.x 基线评测 - 固定长度分块，无优化"

data:
  meal: "test1"
  create_if_missing:
    sample_ratio: 0.05
    seed: 42

test_sets:
  - strategy: "document"
    num_questions: 35


variants:
  - name: "baseline"
    description: "baseline without any overrides. (chunk_size: 512, chunk_overlap: 0)"
    config_overrides: {}

evaluation:
  llm_preset: "default"
  metrics:
    retrieval:
      - "hit_rate"
      - "mrr"
      - "ndcg"
    generation:
      - "faithfulness"
      - "answer_relevancy"

llm:
  question_generation: "default"
  answering: "default"
