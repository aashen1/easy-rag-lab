# 代码质量修复计划 - Phase 4 测试质量改进

> 创建日期：2026-05-11 | 目标：完成 Phase 4 测试质量改进任务

***

## 当前状态分析

### 已完成阶段

* ✅ **Phase 1: 止血（P0 严重问题）** - 100% 完成

* ✅ **Phase 2: 瘦身（P1 臃肿与重复）** - 100% 完成

### 待完成阶段

* ❌ **Phase 3: 治本（P2-P3 架构问题）** - 0% 完成（架构重构，风险较高）

* ⚠️ **Phase 4: 测试质量（P5 测试问题）** - \~30% 完成

* ❌ **Phase 5: 清理（P4-P6 其他问题）** - 0% 完成

***

## 本计划目标

**选择原则**：量力而行，选择风险可控、收益明显、有信心一次完成的任务。

**选择范围**：Phase 4 测试质量改进 - 参数化重构任务

**预期收益**：

* 减少测试代码重复

* 提升测试可维护性

* 降低测试行数 50%+

* 为后续测试改进奠定基础

***

## 实施计划

### 任务 1: 重构 `TestToolFunctions` + `TestNewTools` (23→1-2)

**文件**: `tests/test_agent.py`

**当前状态**:

* `TestToolFunctions`: 10 个测试方法（工具存在性测试）

* `TestNewTools`: 13 个测试方法（新工具存在性测试 + mock 测试）

* 总计 **23 个测试**，全部是 `.name` 断言模式或 mock 测试

**目标**: 合并为 1-2 个参数化测试

**步骤**:

1. 分析 23 个测试的模式

   * 识别所有工具函数名称

   * 提取测试数据（工具名称、描述文本、是否高风险）

   * 区分存在性测试和 mock 测试
2. 创建参数化测试

   * 使用 `@pytest.mark.parametrize`

   * 定义测试数据列表

   * 合并 23 个测试为 1-2 个（存在性测试 + mock 测试）
3. 验证功能

   * 运行测试确保通过

   * 确认覆盖率不变

**预计耗时**: 2 小时

**验收标准**:

* [ ] 测试数量从 23 减少到 1-2 个

* [ ] 所有测试通过

* [ ] 代码覆盖率不变

***

### 任务 2: 重构 `TestBuildSystemPrompt` (10→1)

**文件**: `tests/test_agent.py`

**当前状态**: 10 个测试方法，全部是字符串包含测试模式

**目标**: 合并为 1 个参数化测试

**步骤**:

1. 分析 10 个测试的模式

   * 识别输入条件

   * 提取期望包含的字符串
2. 创建参数化测试

   * 使用 `@pytest.mark.parametrize`

   * 定义测试数据（条件、期望字符串）

   * 合并 10 个测试为 1 个
3. 验证功能

   * 运行测试确保通过

   * 确认覆盖率不变

**预计耗时**: 1.5 小时

**验收标准**:

* [ ] 测试数量从 10 减少到 1 个

* [ ] 所有测试通过

* [ ] 代码覆盖率不变

***

### 任务 3: 重构 `TestCLICommands` (9→1)

**文件**: `tests/test_agent.py`

**当前状态**: 9 个测试方法，全部是命令存在性测试模式

**目标**: 合并为 1 个参数化测试

**步骤**:

1. 分析 9 个测试的模式

   * 识别命令名称

   * 提取命令描述
2. 创建参数化测试

   * 使用 `@pytest.mark.parametrize`

   * 定义测试数据（命令名称、描述）

   * 合并 9 个测试为 1 个
3. 验证功能

   * 运行测试确保通过

   * 确认覆盖率不变

**预计耗时**: 1 小时

**验收标准**:

* [ ] 测试数量从 9 减少到 1 个

* [ ] 所有测试通过

* [ ] 代码覆盖率不变

***

### 任务 4: 重构 `TestChunkTextChineseRoundtrip` (7→1)

**文件**: `tests/test_chunker.py`

**当前状态**: 7 个测试方法，全部是 roundtrip 测试模式

**目标**: 合并为 1 个参数化测试

**步骤**:

1. 分析 7 个测试的模式

   * 识别输入文本

   * 提取分块参数
2. 创建参数化测试

   * 使用 `@pytest.mark.parametrize`

   * 定义测试数据（文本、参数）

   * 合并 7 个测试为 1 个
3. 验证功能

   * 运行测试确保通过

   * 确认覆盖率不变

**预计耗时**: 1 小时

**验收标准**:

* [ ] 测试数量从 7 减少到 1 个

* [ ] 所有测试通过

* [ ] 代码覆盖率不变

***

### 任务 5: 重构 `TestRagasEvaluatorConfigReading` (12→1)

**文件**: `tests/test_evaluators.py`

**当前状态**: 12 个测试方法，全部是配置读取测试模式

**目标**: 合并为 1 个参数化测试

**步骤**:

1. 分析 12 个测试的模式

   * 识别配置键

   * 提取期望值

   * 区分 run\_config 和 embedding 配置
2. 创建参数化测试

   * 使用 `@pytest.mark.parametrize`

   * 定义测试数据（配置键、期望值）

   * 合并 12 个测试为 1 个
3. 验证功能

   * 运行测试确保通过

   * 确认覆盖率不变

**预计耗时**: 2 小时

**验收标准**:

* [ ] 测试数量从 12 减少到 1 个

* [ ] 所有测试通过

* [ ] 代码覆盖率不变

***

## 实施顺序

按风险从低到高排序：

1. **任务 3**: `TestCLICommands` (9→1) - 简单，模式清晰
2. **任务 2**: `TestBuildSystemPrompt` (10→1) - 简单，模式清晰
3. **任务 4**: `TestChunkTextChineseRoundtrip` (7→1) - 中等，需要理解分块逻辑
4. **任务 5**: `TestRagasEvaluatorConfigReading` (12→1) - 中等，需要区分配置类型
5. **任务 1**: `TestToolFunctions` + `TestNewTools` (23→1-2) - 最复杂，数量最多，需要区分测试类型

***

## 风险评估

### 低风险

* 所有任务都是测试重构，不影响生产代码

* 参数化是成熟的测试模式

* 可以逐个任务验证

### 潜在问题

* 测试数据提取可能遗漏边界情况

* 参数化后测试失败信息可能不够清晰

* 需要确保覆盖率不降低

### 缓解措施

* 每个任务完成后立即运行测试验证

* 保留原始测试逻辑，只改变组织方式

* 使用清晰的测试 ID 标识参数化用例

***

## 预期成果

### 量化指标

* **测试数量**: 从 61 个减少到 5-6 个（减少 90%）

* **代码行数**: 预计减少 300+ 行

* **维护成本**: 显著降低

* **可读性**: 显著提升

### 质量提升

* 消除重复代码

* 统一测试模式

* 提升测试可维护性

* 为后续测试改进奠定基础

***

## 验收标准

### 每个 Task 的验收标准

* [ ] 测试数量减少到目标值

* [ ] 所有测试通过 (`pixi run test`)

* [ ] Lint 检查通过 (`pixi run lint`)

* [ ] 代码覆盖率不变或提升

### 最终验收标准

* [ ] 5 个任务全部完成

* [ ] 测试代码行数减少 200+ 行

* [ ] 无测试失败

* [ ] 无 lint 错误

* [ ] 更新进度文件

***

## 后续建议

完成本计划后，建议继续：

1. **Phase 4 剩余任务**:

   * 为工具函数添加行为测试

   * 减少 `test_pipeline.py` 的 mock 层数

   * 补充关键缺失测试

2. **Phase 5 清理任务**:

   * 删除遗留代码

   * 修复代码异味

   * 统一 JSON 提取正则

3. **Phase 3 架构重构**（风险较高，建议最后进行）:

   * 修复 Pydantic 模型问题

   * 移除过度设计

   * 统一 dataclass/Pydantic 使用策略

***

## 时间估算

| 任务     | 预计耗时       | 风险等级    |
| ------ | ---------- | ------- |
| 任务 1   | 2 小时       | 中       |
| 任务 2   | 1.5 小时     | 低       |
| 任务 3   | 1 小时       | 低       |
| 任务 4   | 1 小时       | 低       |
| 任务 5   | 2 小时       | 中       |
| **总计** | **7.5 小时** | **低-中** |

***

## 备注

* 本计划专注于测试质量改进，不涉及生产代码修改

* 所有任务都有明确的验收标准

* 建议按顺序执行，每个任务完成后立即验证

* 如遇到意外问题，可以随时调整计划

